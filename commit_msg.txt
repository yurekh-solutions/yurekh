Fix fixed header overlap on all pages using AppLayout

- Added pt-[110px] to main element in AppLayout to account for the
  fixed header height (top bar ~40px + nav bar ~70px)
- Prevents page content from hiding behind the fixed header
- Index page unaffected (includes Header directly)
