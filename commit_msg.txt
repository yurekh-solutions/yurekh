Proxy /api/* to AINOS backend so dashboard API calls work

AINOS pages fetch /api/... without the /ainos basePath; when browsing via
this domain those requests hit the SPA and returned HTML (405 on POST).
Route /api/* to ainos-ywu0.onrender.com/ainos/api/* on Vercel.
