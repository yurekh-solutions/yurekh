Speed up page navigation with SPA routing, chunk prefetch, and instant scroll

- Header desktop nav: logo, top-level items, Company dropdown, and Industries mega menu now use SPA navigate() instead of full page reloads
- App.tsx: prefetch all lazy page chunks during browser idle time so route changes render instantly
- ScrollToTop: instant scroll instead of slow smooth scroll on route change
